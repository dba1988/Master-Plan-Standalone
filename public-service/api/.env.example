# Public Service API Configuration

# Server
PORT=8001
HOST=0.0.0.0

# Database (read-only connection)
# Use port 5433 if running via docker-compose (mapped from internal 5432)
DATABASE_URL=postgres://masterplan:masterplan_dev@localhost:5433/masterplan

# CDN Base URL (R2 public access URL)
# Get this from Cloudflare Dashboard after enabling public access
CDN_BASE_URL=https://pub-xxxxx.r2.dev

# Client API (external CRM/webhook integration)
CLIENT_API_URL=
CLIENT_API_KEY=
CLIENT_API_TIMEOUT=10000

# App settings
DEBUG=true
